<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
          package="com.oleg.memoreitapp">

    <application
            tools:replace="android:icon"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".booked.BookedActivity">
        </activity>
        <activity android:name=".payment.PaymentActivity">
        </activity>
        <activity android:name=".add_info.AddInfoActivity"/>
        <activity android:name=".review_booking.ReviewBookingActivity"/>
        <activity android:name=".pick_date.PickDateActivity"/>
        <activity android:name=".find_photographer.FindPhotographerActivity"/>
        <activity android:name=".MainActivity"/>
        <activity
                android:name=".find_city.FindCityActivity"
                android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>